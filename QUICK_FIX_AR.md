# 🔧 إصلاح سريع لمشكلة "غير مصرح به"

## ⚡ الحل السريع (3 خطوات فقط)

### الطريقة 1: إصلاح تلقائي ⚙️

1. **افتح Console في المتصفح**
   - اضغط `F12` على Windows/Linux
   - أو `Cmd + Option + J` على Mac

2. **انسخ والصق هذا الكود:**
   ```javascript
   const user = JSON.parse(localStorage.getItem('user') || '{}');
   const token = localStorage.getItem('token');
   if (user && token) {
     user.token = token;
     localStorage.setItem('user', JSON.stringify(user));
     alert('✅ تم الإصلاح! قم بتحديث الصفحة الآن.');
     location.reload();
   } else {
     localStorage.clear();
     alert('⚠️ يرجى تسجيل الدخول مرة أخرى');
     location.href = '/login';
   }
   ```

3. **اضغط Enter** - سيتم تحديث الصفحة تلقائياً ✅

---

### الطريقة 2: مسح البيانات وإعادة تسجيل الدخول 🔄

**إذا لم تنجح الطريقة الأولى:**

1. **افتح Console** (F12)

2. **امسح البيانات:**
   ```javascript
   localStorage.clear();
   sessionStorage.clear();
   ```

3. **حدث الصفحة:** اضغط `Ctrl + Shift + R`

4. **سجل الدخول مرة أخرى** في `/login`

---

## ✅ اختبار الإصلاح

بعد الإصلاح، افتح Console وشغل:

```javascript
const user = JSON.parse(localStorage.getItem('user'));
console.log('✅ اسم المستخدم:', user?.name);
console.log('✅ الدور:', user?.role);
console.log('✅ يوجد Token:', !!user?.token);
```

يجب أن يظهر:
```
✅ اسم المستخدم: [اسمك]
✅ الدور: teacher / student / admin
✅ يوجد Token: true
```

---

## 🎯 الآن يمكنك

- ✅ إنشاء الدورات بدون مشاكل
- ✅ رفع الملفات
- ✅ استخدام جميع ميزات المنصة

---

## ❓ مشاكل؟

إذا استمرت المشكلة:

1. امسح الـ cache:
   - Chrome: `Ctrl + Shift + Delete`
   - Firefox: `Ctrl + Shift + Delete`

2. أعد تشغيل المتصفح

3. سجل الدخول مرة أخرى

---

**تم الإصلاح بنجاح! 🎉**
