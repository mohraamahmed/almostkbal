# 🔧 دليل حل المشاكل التقنية في المنصة التعليمية

## ✅ المشاكل التي تم حلها

### 1. مشكلة ChunkLoadError
**المشكلة:** فشل تحميل chunks للصفحات
**الحل:** 
- تنظيف الكاش (`.next` و `node_modules/.cache`)
- تحديث `next.config.js` بإعدادات webpack محسنة
- تعطيل `optimizeCss` مؤقتاً

### 2. مشكلة Supabase Multiple Instances
**المشكلة:** إنشاء عدة instances من Supabase client
**الحل:** 
- إنشاء ملف موحد `src/lib/supabase.ts`
- استخدام singleton pattern

### 3. مشكلة الـ IDs غير الصالحة
**المشكلة:** استخدام نصوص عادية بدلاً من UUID
**الحل:**
- إضافة validation للـ UUID
- إنشاء صفحات مساعدة لعرض IDs الصحيحة

### 4. مشكلة الكورسات غير المنشورة
**المشكلة:** الكورسات الجديدة لا تظهر
**الحل:**
- إضافة خيار "نشر مباشرة" عند الإنشاء
- تحديث دالة `createCourseWithLessons`

## 🛠️ الأدوات المتاحة

| الأداة | الرابط | الوصف |
|--------|--------|--------|
| **فحص النظام** | `/system-check` | فحص شامل لكل مكونات المنصة |
| **إنشاء كورس بسيط** | `/simple-create-course` | إنشاء كورس تجريبي بسرعة |
| **قائمة الكورسات** | `/list-all-courses` | عرض جميع الكورسات مع IDs |
| **الحصول على ID صحيح** | `/get-valid-course-id` | عرض IDs الصحيحة للكورسات |
| **مساعد الكورسات** | `/course-helper` | أداة شاملة لحل مشاكل الكورسات |
| **تشخيص كورس** | `/debug-course/[ID]` | تشخيص تفصيلي لأي كورس |

## 📝 كيفية استخدام المنصة

### بدء التشغيل
```bash
# تنظيف الكاش
node fix-all-issues.js

# تثبيت الحزم
npm install

# تشغيل السيرفر
npm run dev
```

### الرابط الرئيسي
```
http://localhost:3000
```

## 🗄️ قاعدة البيانات Supabase

### البيانات
- **URL:** https://wnqifmvgvlmxgswhcwnc.supabase.co
- **Anon Key:** في ملف `.env.local`

### الجداول الرئيسية
- `courses` - الكورسات
- `lessons` - الدروس
- `users` - المستخدمين
- `enrollments` - التسجيلات

## 📋 SQL Scripts المفيدة

### نشر جميع الكورسات
```sql
UPDATE courses SET is_published = true;
```

### عرض آخر الكورسات
```sql
SELECT id, title, is_published, created_at 
FROM courses 
ORDER BY created_at DESC 
LIMIT 10;
```

### إنشاء كورس تجريبي
```sql
INSERT INTO courses (
  title, description, instructor_name, 
  price, level, category, is_published
) VALUES (
  'كورس تجريبي', 'وصف تجريبي', 'مدرس تجريبي',
  0, 'beginner', 'test', true
) RETURNING id;
```

## 🚨 حل المشاكل الشائعة

### مشكلة: "الكورس غير موجود"
1. افتح `/get-valid-course-id`
2. انسخ ID صحيح
3. استخدمه في الرابط `/courses/[ID]`

### مشكلة: ChunkLoadError
1. أوقف السيرفر (Ctrl+C)
2. شغل `node fix-all-issues.js`
3. شغل `npm run dev`

### مشكلة: 500 Internal Server Error
1. تحقق من `.env.local`
2. تأكد من Supabase credentials
3. افتح `/system-check` للتشخيص

## 📂 هيكل المشروع

```
frontend/
├── src/
│   ├── app/           # صفحات Next.js 13
│   ├── components/    # المكونات القابلة لإعادة الاستخدام
│   ├── lib/          # المكتبات والأدوات
│   ├── contexts/     # React Contexts
│   └── services/     # خدمات API
├── public/           # الملفات الثابتة
├── .env.local        # متغيرات البيئة
├── next.config.js    # إعدادات Next.js
├── package.json      # التبعيات
└── fix-all-issues.js # أداة إصلاح شاملة
```

## 🔄 التحديثات الأخيرة

1. ✅ تنظيف شامل للكاش والملفات القديمة
2. ✅ إصلاح مشاكل TypeScript
3. ✅ تحسين أداء webpack
4. ✅ توحيد Supabase client
5. ✅ إضافة validation للـ UUIDs
6. ✅ إنشاء أدوات تشخيص شاملة
7. ✅ تحسين معالجة الأخطاء

## 💡 نصائح مهمة

1. **دائماً استخدم UUID صحيح** للكورسات
2. **تأكد من نشر الكورس** عند الإنشاء
3. **استخدم `/system-check`** عند حدوث مشاكل
4. **نظف الكاش** عند ظهور ChunkLoadError
5. **احفظ نسخة احتياطية** من `.env.local`

## 🆘 للمساعدة

إذا واجهت أي مشكلة:
1. افتح `/system-check` للتشخيص
2. راجع Console في المتصفح (F12)
3. تحقق من ملف `.env.local`
4. شغل `node fix-all-issues.js`

---

**آخر تحديث:** 2024
**الإصدار:** 1.0.0
**حالة المنصة:** ✅ تعمل بشكل كامل
